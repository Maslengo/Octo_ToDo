жетон вов
переделать иконки слева (как на скрине на раб столе)
мб переделать сервер в одельную строку? (со своим шрифтом) framepool
COLLECT_ALL_инвентарь
if point then
	-- OctoTooltip:SetOwner(frame, "ANCHOR_NONE")
	-- local first, second = unpack(point)
	-- print (first, second)
	-- OctoTooltip:SetPoint(first, frame, second)
	-- else
	--     OctoTooltip:SetOwner(frame, "ANCHOR_CURSOR_RIGHT")
end